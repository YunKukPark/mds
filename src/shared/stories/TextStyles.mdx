import { Meta, Typeset } from '@storybook/blocks';
import theme from '../theme';

<Meta title="Typography/Base" />

# Typography

**Font:** Noto Sans KR

**Weights:** 400(regular), 500(medium), 700(bold), 800(extrabold), 900(black)

## Headers

<Typeset
  fontSizes={[
    parseInt(theme.fontSizes.h1),
    parseInt(theme.fontSizes.h2),
    parseInt(theme.fontSizes.h3),
    parseInt(theme.fontSizes.h4),
    parseInt(theme.fontSizes.h5),
    parseInt(theme.fontSizes.h6),
  ]}
  sampleText="다람쥐 헌 쳇바퀴에 타고파"
  fontWeight={700}
/>

## Body

<Typeset
  fontSizes={[
    parseInt(theme.fontSizes.body1),
    parseInt(theme.fontSizes.body2),
    parseInt(theme.fontSizes.body3),
  ]}
  sampleText="다람쥐 헌 쳇바퀴에 타고파"
  fontWeight={400}
/>

## Others

<Typeset
  fontSizes={[
    parseInt(theme.fontSizes.caption),
    parseInt(theme.fontSizes.buttonLg),
    parseInt(theme.fontSizes.buttonMd),
    parseInt(theme.fontSizes.buttonSm),
    parseInt(theme.fontSizes.label),
    parseInt(theme.fontSizes.chip),
  ]}
  sampleText="다람쥐 헌 쳇바퀴에 타고파"
  fontWeight={400}
/>

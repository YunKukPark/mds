import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';

import { theme } from '@shared/theme';

<Meta title="theme/Colors" />

# Colors

<ColorPalette>
  {Object.entries(theme.colors).map(([title, colors]) => (
    <ColorItem key={title} title={title} colors={colors} />
  ))}
</ColorPalette>
